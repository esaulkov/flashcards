= simple_form_for(@<%= singular_table_name %>) do |f|
  = f.error_notification

  .form-inputs
  <%- attributes.each do |attr| -%>
    = f.<%= attr.reference? ? :association : :input %> :<%= attr.name %>
  <%- end -%>

  .form-actions
    = f.button :submit
