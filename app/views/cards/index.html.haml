%h3= t "All cards"
%table.table.table-striped
  %thead
    %tr
      %th= t "Original text"
      %th= t "Translated text"
      %th= t "Image"
      %th= t "Review date"
      %th= t "Deck"
      %th{ colspan: "2" }
  %tbody
    - @cards.each do |card|
      %tr
        %td= card.original_text
        %td= card.translated_text
        %td= image_tag card.image.url(:thumb)
        %td= l card.review_date
        %td= link_to card.deck.name, deck_path(card.deck)
        %td= link_to t("Edit"), edit_card_path(card)
        %td= link_to t("Delete"), card, method: :delete, data: { confirm: t("Are you sure?") }
